# Client logos

Drop client logo image files here (PNG, SVG, or JPG). Then reference the filename in [`data/clients.json`](../../data/clients.json) — see [`data/README.md`](../../data/README.md) for the exact steps.

Nothing in this folder appears on the site until it's referenced in `data/clients.json`.
